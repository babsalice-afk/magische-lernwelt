import { SUPABASE_URL, SUPABASE_ANON_KEY } from "./config.js";
const $=s=>document.querySelector(s), uid=()=>crypto.randomUUID(), now=()=>new Date().toISOString();
const demo=!(SUPABASE_URL&&SUPABASE_ANON_KEY);
const SUBJECTS=[{id:"de",name:"Deutsch",icon:"📖",enabled:true},{id:"ma",name:"Mathematik",icon:"➕",enabled:false},{id:"en",name:"Englisch",icon:"🌙",enabled:false},{id:"su",name:"Sachunterricht",icon:"🌍",enabled:false}];
const COLLECTION=[
 ["mooncat","Mondkatze","🐈‍⬛","Tier","selten",1],["mossbunny","Mooshase","🐇","Tier","gewöhnlich",1],["cloudfox","Wolkenfuchs","🦊","Tier","selten",0],["owl","Runeneule","🦉","Tier","gewöhnlich",0],["unicorn","Nebel-Einhorn","🦄","Fabelwesen","legendär",0],["crystalstag","Kristallhirsch","🦌","Tier","episch",0],
 ["stardragon","Sternendrache","🐉","Drache","legendär",0],["emberdragon","Glutdrache","🐲","Drache","episch",0],["icedragon","Frostdrache","🐉","Drache","episch",0],["babydragon","Funkeldrache","🐲","Drache","selten",0],
 ["flowerfairy","Blütenfee","🧚","Fee","selten",0],["moonfairy","Mondfee","🧚‍♀️","Fee","episch",0],["starfairy","Sternenfee","🧚","Fee","legendär",0],["forestelf","Waldelf","🧝","Elf","selten",0],["starelf","Sternenelfe","🧝‍♀️","Elf","episch",0],["moonelf","Mondelf","🧝","Elf","legendär",0],
 ["pumpkin","Kürbisgeist","🎃","Herbstwesen","gewöhnlich",0],["ghost","Laternengeist","👻","Geist","selten",0],["forestspirit","Waldgeist","🌱","Geist","selten",0],["crystalspirit","Kristallgeist","🔮","Geist","episch",0],["snowspirit","Schneeflöckchen","☃️","Winterwesen","selten",0],["starbird","Sternenvogel","🦜","Tier","selten",0],["moonmoth","Mondfalter","🦋","Fabelwesen","gewöhnlich",0],["seasprite","Wasserfunke","🧜‍♀️","Wasserwesen","episch",0]
].map(x=>({id:x[0],name:x[1],icon:x[2],type:x[3],rarity:x[4],unlocked:!!x[5],starCost:({gewöhnlich:25,selten:40,episch:60,legendär:90}[x[4]]||40)}));
const CATALOG=[
 ["moonbed","Wohnraum","Mondbett","🛏️",18,"house"],["cloudsofa","Wohnraum","Wolkensofa","🛋️",14,"house"],["startable","Wohnraum","Sternentisch","🪵",10,"house"],["crystalchair","Wohnraum","Kristallsessel","🪑",12,"house"],["starrug","Wohnraum","Sternenteppich","🧶",8,"house"],["books","Wohnraum","Zauberbibliothek","📚",12,"house"],["fireplace","Wohnraum","Kristallkamin","🔥",18,"house"],["music","Wohnraum","Mondharfe","🎵",14,"house"],["tea","Wohnraum","Feen-Teeservice","🫖",7,"house"],["cushions","Wohnraum","Sternenkissen","🪷",6,"house"],
 ["moonwindow","Haus","Mondfenster","🪟",12,"house"],["runedoor","Haus","Runentür","🚪",12,"house"],["starroof","Haus","Sternendach","🌟",20,"house"],["vinewall","Haus","Blütenranken","🌿",9,"house"],["fairylights","Haus","Feenlichter","✨",8,"house"],["portrait","Haus","Magisches Bild","🖼️",10,"house"],
 ["moonflowers","Garten","Mondblumen","🌸",6,"garden"],["crystalbed","Garten","Kristallbeet","💎",8,"garden"],["fairypond","Garten","Feenteich","🪷",14,"garden"],["forestbench","Garten","Wald-Bank","🪑",8,"garden"],["mushroomring","Garten","Pilzring","🍄",10,"garden"],["moonfountain","Garten","Mondbrunnen","⛲",18,"garden"],["herbgarden","Garten","Zauberkräuter","🌱",7,"garden"],["rosearch","Garten","Rosenbogen","🌹",11,"garden"],
 ["lanterns","Außen","Zauberlaternen","🏮",8,"all"],["fireflies","Außen","Glühwürmchen","✨",6,"all"],["bridge","Außen","Holzbrücke","🌉",15,"waterfall"],["campfire","Außen","Magisches Lagerfeuer","🔥",10,"clearing"],["crystals","Außen","Leuchtkristalle","💠",10,"crystal"],["dragonnest","Außen","Drachennest","🪺",16,"dragon"],["elflamp","Außen","Elfenlaterne","🕯️",8,"elf"],["fairyhouse","Außen","Feenhäuschen","🏡",13,"fairy"],
 ["pumpkins","Saisonal","Leuchtkürbisse","🎃",8,"garden"],["ghostlamp","Saisonal","Geisterlaterne","👻",9,"all"],["winter","Saisonal","Winterzauber","❄️",12,"all"],["snowman","Saisonal","Schneewesen","☃️",10,"garden"],["spring","Saisonal","Frühlingsblüten","🌷",9,"all"],["starsnow","Saisonal","Sternenschnee","🌨️",14,"all"]
].map(x=>({id:x[0],cat:x[1],name:x[2],icon:x[3],cost:x[4],zone:x[5]}));
const AREAS={
  house:{name:"Mein Zauberhaus",icon:"🏡",desc:"Dein eigenes magisches Zuhause. Richte Zimmer ein und sammle besondere Möbel."},
  clearing:{name:"Magische Lichtung",icon:"✨",desc:"Eine ruhige Lichtung für Lagerfeuer, Glühwürmchen und magische Dekoration."},
  waterfall:{name:"Wasserfall",icon:"💧",desc:"Ein verwunschener Wasserfall mit Brücken, Pflanzen und leuchtenden Wasserfunken."},
  crystal:{name:"Kristallhöhle",icon:"💎",desc:"Eine funkelnde Höhle für Kristalle und geheimnisvolle Bewohner."},
  dragon:{name:"Drachenberg",icon:"🐉",desc:"Hoch oben am Berg können Drachen wohnen und besondere Nester gestaltet werden."},
  elf:{name:"Elfendorf",icon:"🧝",desc:"Ein kleines Dorf mit Laternen, Häusern und Elfenbewohnern."},
  fairy:{name:"Feengarten",icon:"🧚",desc:"Ein heller Garten voller Blumen, Feenlichter und kleiner Häuschen."},
  garden:{name:"Garten",icon:"🌸",desc:"Dein persönlicher Garten für Beete, Brunnen, Rosenbögen und saisonale Dekoration."}
};
const defaultState={family:{name:"Unsere Lernwelt"},children:[],subjects:SUBJECTS,sets:[],rewards:[],collection:COLLECTION,catalog:CATALOG,world:{level:2,owned:["fireflies"],placed:{house:["fireflies"],clearing:[],waterfall:[],crystal:[],dragon:[],elf:[],fairy:[],garden:[]},area:"clearing",room:"Wohnzimmer",testGrantV4:false},history:[]};
let state=JSON.parse(localStorage.getItem("mlw-v2")||"null")||structuredClone(defaultState);
let route="home", currentChild=state.children[0]?.id||null, supabase=null, session=null, syncTimer=null, catalogCat="Alle", selectedArea="clearing";
const esc=(s="")=>String(s).replace(/[&<>"']/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[m]));
const child=()=>state.children.find(x=>x.id===currentChild), sets=()=>state.sets.filter(x=>x.childId===currentChild);
function normalize(){state={...structuredClone(defaultState),...state,family:{...defaultState.family,...(state.family||{})},subjects:state.subjects?.length?state.subjects:structuredClone(SUBJECTS),children:state.children||[],sets:state.sets||[],rewards:state.rewards||[],history:state.history||[]};state.collection=COLLECTION.map(b=>({...b,...((state.collection||[]).find(x=>x.id===b.id)||{})}));state.catalog=CATALOG.map(b=>({...b,...((state.catalog||[]).find(x=>x.id===b.id)||{})}));state.world={...structuredClone(defaultState.world),...(state.world||{}),placed:{...structuredClone(defaultState.world.placed),...(state.world?.placed||{})}};if(!state.world.testGrantV4){state.children.forEach(c=>c.crystals=Math.max(c.crystals||0,1500));state.world.testGrantV4=true}}
normalize();
function saveLocal(){localStorage.setItem("mlw-v2",JSON.stringify(state))} function scheduleSync(){saveLocal();clearTimeout(syncTimer);syncTimer=setTimeout(pushCloud,500)}
async function initCloud(){if(demo)return;try{const {createClient}=await import("https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm");supabase=createClient(SUPABASE_URL,SUPABASE_ANON_KEY);const r=await supabase.auth.getSession();session=r.data.session;if(session)await pullCloud()}catch(e){console.warn(e)}}
async function pullCloud(){if(!supabase||!session)return;const r=await supabase.from("app_state").select("payload").eq("user_id",session.user.id).maybeSingle();if(r.data?.payload){state={...structuredClone(defaultState),...r.data.payload};normalize();currentChild=state.children[0]?.id||null;saveLocal()}}
async function pushCloud(){if(!supabase||!session)return;await supabase.from("app_state").upsert({user_id:session.user.id,payload:state,updated_at:now()},{onConflict:"user_id"})}
function topnav(calm=false){let c=child();return `<header class="magicTop ${calm?'calmTop':''}"><button class="profileChip" data-route="home"><span class="avatar">${(c?.name||'?').slice(0,1)}</span><b>${esc(c?.name||'Profil')}</b></button><nav>${[["learn","📖 Lernen"],["world","🌳 Zauberwelt"],["collection","✨ Sammlung"],["design","🪄 Gestalten"],["parent","⚙️ Eltern"]].map(([r,n])=>`<button data-route="${r}" class="${route===r?'active':''}">${n}</button>`).join("")}</nav><div class="currency">⭐ ${c?.stars||0} <span>💎 ${c?.crystals||0}</span></div></header>`}
function shell(body,calm=false){return `<div class="shell fantasyShell">${topnav(calm)}${body}</div>`}
function render(){normalize();let b=route==="home"?home():route==="learn"?learnHome():route==="world"?world():route==="place"?placeView(selectedArea):route==="collection"?collectionView():route==="design"?designView():parent();$("#app").innerHTML=shell(b,route==="learn");bind()}
function profileBar(){return state.children.length?`<div class="row wrap">${state.children.map(c=>`<button class="pill" data-child="${c.id}">${c.id===currentChild?'✓ ':''}${esc(c.name)}</button>`).join("")}</div>`:`<div class="card"><b>Noch kein Kinderprofil.</b><div class="small">Im Elternbereich kannst du das erste Profil anlegen.</div></div>`}
function home(){let c=child();return `<main class="page"><section class="magicHero"><span class="tag">✦ Persönliche Lernreise</span><h1>Willkommen in deiner magischen Lernwelt.</h1><p>Lernen, sammeln und deine eigene Zauberwelt gestalten.</p>${profileBar()}<div class="heroActions"><button class="btn gold" data-route="learn">📖 Lernen</button><button class="btn" data-route="world">🌳 Zauberwelt öffnen</button></div></section><section class="statGrid"><div class="glassCard"><span>📚</span><b>${sets().length}</b><small>Lernlisten</small></div><div class="glassCard"><span>⭐</span><b>${c?.stars||0}</b><small>Sterne</small></div><div class="glassCard"><span>💎</span><b>${c?.crystals||0}</b><small>Kristalle</small></div><div class="glassCard"><span>✨</span><b>${state.collection.filter(x=>x.unlocked).length}/${state.collection.length}</b><small>Entdeckt</small></div></section><section class="sectionTitle"><h2>Deine Fächer</h2></section><section class="subjectGrid">${state.subjects.map(s=>`<div class="subjectCard ${s.enabled?'':'locked'}"><div class="subjectIcon">${s.icon}</div><h2>${s.name}</h2><p>${s.enabled?'Lernwörter, Rechtschreibung und adaptives Üben':'Modul ist für die spätere Erweiterung vorbereitet'}</p><span class="tag">${s.enabled?'aktiv':'vorbereitet'}</span></div>`).join("")}</section></main>`}
function learnHome(){return `<main class="page learnPage"><section class="learnHero"><span class="tag">Ruhiger Lernmodus</span><h1>Wie möchtest du lernen?</h1><p>Du kannst die Lernstrategie und das Lernspiel selbst auswählen – oder die App adaptiv entscheiden lassen.</p>${profileBar()}</section><section class="learnGrid">${sets().map(s=>{let a=Math.round(s.items.reduce((n,x)=>n+x.mastery,0)/Math.max(1,s.items.length));return `<div class="learnCard"><div class="bookMark">📖</div><h2>${esc(s.title)}</h2><div class="progress"><i style="width:${a}%"></i></div><p>${s.items.length} Wörter · ${a}% sicher</p><button class="btn" data-configure="${s.id}">Lernart & Spiel wählen</button></div>`}).join("")||`<div class="learnCard"><h2>Noch keine Lernliste</h2><p>Im Elternbereich kannst du Lernwörter selbst eingeben.</p><button class="btn light" data-route="parent">Zum Elternbereich</button></div>`}</section></main>`}
function pickByStrategy(set,strategy,index){let items=[...set.items];if(strategy==="weak")items=items.filter(x=>(x.mastery||0)<60).length?items.filter(x=>(x.mastery||0)<60):items;if(strategy==="order")return items[index%items.length];return items.map(it=>({it,score:100-(it.mastery||0)+(it.wrong||0)*6+Math.random()*8})).sort((a,b)=>b.score-a.score)[0]?.it}
function adaptiveMode(it){let m=it.mastery||0;return m<35?["recognize","scramble"][Math.floor(Math.random()*2)]:m<70?["gap","scramble","recognize"][Math.floor(Math.random()*3)]:["memory","gap","recognize","scramble"][Math.floor(Math.random()*4)]}
function scramble(w){let a=[...w],tries=0;do{a.sort(()=>Math.random()-.5);tries++}while(a.join("")===w&&tries<8);return a.join(" · ")} function gap(w){return [...w].map((c,i)=>i>0&&i<w.length-1&&i%3===1?"_":c).join(" ")} function distract(w){let a=w.length>3?w.slice(0,2)+w.slice(3):w+"h",b=w.length>4?w.slice(0,-2)+w.at(-1)+w.at(-2):w+"e";return [...new Set([w,a,b])].sort(()=>Math.random()-.5)}
function configureLearning(id){let set=state.sets.find(x=>x.id===id);if(!set)return;$("#app").innerHTML=`<div class="learnSession"><div class="learnbox chooseBox"><button class="btn light" id="backLearn">← Zurück</button><h1>${esc(set.title)}</h1><p class="muted">1. Lernvariante wählen</p><div class="optionGrid"><button class="option selected" data-strategy="adaptive"><b>✨ Adaptiv</b><span>Schwierige Wörter häufiger</span></button><button class="option" data-strategy="weak"><b>🎯 Unsichere Wörter</b><span>Fokus unter 60 %</span></button><button class="option" data-strategy="order"><b>📚 Alle der Reihe nach</b><span>Jedes Wort systematisch</span></button></div><p class="muted">2. Lernspiel wählen</p><div class="gameGrid"><button class="game selected" data-game="adaptive"><b>🪄 Magischer Mix</b><span>Wechselt zwischen den interaktiven Spielen</span></button><button class="game" data-game="recognize"><b>🔎 Wortdetektiv</b><span>Richtiges Wort antippen</span></button><button class="game" data-game="scramble"><b>🔤 Buchstabensalat</b><span>Buchstaben in Reihenfolge antippen</span></button><button class="game" data-game="gap"><b>🧩 Lückenwort</b><span>Passende Buchstaben auswählen</span></button><button class="game" data-game="memory"><b>🌙 Merkzauber</b><span>Wort merken und danach wiedererkennen</span></button></div><div class="row between" style="margin-top:22px"><span class="small">Kein Tippen nötig.</span><button class="btn" id="beginLearn">Lernen starten</button></div></div></div>`;let strategy="adaptive",game="adaptive";document.querySelectorAll("[data-strategy]").forEach(b=>b.onclick=()=>{strategy=b.dataset.strategy;document.querySelectorAll("[data-strategy]").forEach(x=>x.classList.toggle("selected",x===b))});document.querySelectorAll("[data-game]").forEach(b=>b.onclick=()=>{game=b.dataset.game;document.querySelectorAll("[data-game]").forEach(x=>x.classList.toggle("selected",x===b))});$("#backLearn").onclick=()=>{route="learn";render()};$("#beginLearn").onclick=()=>startLearning(id,strategy,game)}
function startLearning(id,strategy="adaptive",game="adaptive"){let set=state.sets.find(x=>x.id===id);if(!set||!set.items?.length)return alert("Diese Lernliste enthält noch keine Wörter.");let n=0,earned=0,total=Math.min(12,Math.max(6,set.items.length*2)),it,m,answered=false;
function wordChoices(w){let others=set.items.map(x=>x.text).filter(x=>x!==w&&Math.abs(x.length-w.length)<=3);let variants=distract(w).filter(x=>x!==w);let pool=[...new Set([...others,...variants])].sort(()=>Math.random()-.5);return [...new Set([w,...pool.slice(0,2)])].sort(()=>Math.random()-.5)}
function letterOptions(correct){let alphabet="abcdefghijklmnopqrstuvwxyzäöüß";let pool=[correct.toLocaleLowerCase("de")];while(pool.length<4){let x=alphabet[Math.floor(Math.random()*alphabet.length)];if(!pool.includes(x))pool.push(x)}return pool.sort(()=>Math.random()-.5)}
function draw(){answered=false;it=pickByStrategy(set,strategy,n);m=game==="adaptive"?adaptiveMode(it):game;$("#app").innerHTML=`<div class="learnSession"><div class="learnbox"><div class="row between"><span class="tag">📖 ${esc(set.title)}</span><span>${n+1}/${total}</span></div><div class="progress"><i style="width:${n/total*100}%"></i></div><div id="ex"></div><div class="feedback" id="fb"></div><div class="row between"><button class="btn light" id="quit">Beenden</button><span>⭐ +${earned}</span></div></div></div>`;$("#quit").onclick=()=>{route="learn";render()};exercise()}
function exercise(){let e=$("#ex"),w=it.text;
 if(m==="recognize"){e.innerHTML=`<p class="taskText">Welches Wort ist richtig?</p><div class="choices bigChoices">${wordChoices(w).map(x=>`<button data-word-choice="${esc(x)}">${esc(x)}</button>`).join("")}</div>`;e.querySelectorAll("[data-word-choice]").forEach(b=>b.onclick=()=>ans(b.dataset.wordChoice===w))}
 else if(m==="scramble"){let letters=[...w].map((ch,i)=>({ch,i})).sort(()=>Math.random()-.5),built=[];e.innerHTML=`<p class="taskText">Tippe die Buchstaben in der richtigen Reihenfolge an.</p><div class="tapAnswer" id="tapAnswer">_</div><div class="letterTray">${letters.map(x=>`<button class="letterTile" data-letter-index="${x.i}">${esc(x.ch)}</button>`).join("")}</div><div class="row center"><button class="btn light" id="resetLetters">Neu beginnen</button></div>`;const refresh=()=>{$("#tapAnswer").textContent=built.length?built.map(x=>x.ch).join(""):"_"};e.querySelectorAll("[data-letter-index]").forEach(b=>b.onclick=()=>{if(b.disabled)return;b.disabled=true;built.push({ch:b.textContent,el:b});refresh();if(built.length===letters.length)setTimeout(()=>ans(built.map(x=>x.ch).join("").toLocaleLowerCase("de")===w.toLocaleLowerCase("de")),250)});$("#resetLetters").onclick=()=>{built=[];e.querySelectorAll("[data-letter-index]").forEach(x=>x.disabled=false);refresh()}}
 else if(m==="gap"){let chars=[...w],positions=chars.map((_,i)=>i).filter(i=>i>0&&i<chars.length-1);let count=Math.min(2,Math.max(1,Math.floor(chars.length/4)));positions=positions.sort(()=>Math.random()-.5).slice(0,count).sort((a,b)=>a-b);let step=0,shown=[...chars];positions.forEach(i=>shown[i]="_");const renderGap=()=>{let pos=positions[step],correct=chars[pos];e.innerHTML=`<p class="taskText">Welcher Buchstabe gehört in die Lücke?</p><div class="tapAnswer">${shown.map(esc).join(" ")}</div><div class="letterTray">${letterOptions(correct).map(x=>`<button class="letterTile" data-gap-letter="${esc(x)}">${esc(x)}</button>`).join("")}</div><p class="small centerText">Lücke ${step+1} von ${positions.length}</p>`;e.querySelectorAll("[data-gap-letter]").forEach(b=>b.onclick=()=>{if(b.dataset.gapLetter.toLocaleLowerCase("de")===correct.toLocaleLowerCase("de")){shown[pos]=correct;step++;if(step>=positions.length)setTimeout(()=>ans(true),250);else renderGap()}else{b.classList.add("wrongPick");setTimeout(()=>b.classList.remove("wrongPick"),450)}})};renderGap()}
 else if(m==="memory"){e.innerHTML=`<p class="taskText">Merke dir dieses Wort.</p><div class="memoryWord" id="mem">${esc(w)}</div><div class="memoryTimer"><i id="memoryBar"></i></div><p class="small centerText">Schau es dir in Ruhe an …</p>`;setTimeout(()=>{if(!$("#mem"))return;e.innerHTML=`<p class="taskText">Welches Wort hast du gerade gesehen?</p><div class="choices bigChoices">${wordChoices(w).map(x=>`<button data-memory-choice="${esc(x)}">${esc(x)}</button>`).join("")}</div>`;e.querySelectorAll("[data-memory-choice]").forEach(b=>b.onclick=()=>ans(b.dataset.memoryChoice===w))},4000)}
}
function ans(ok){if(answered)return;answered=true;it.seen=(it.seen||0)+1;it.last=now();if(ok){it.correct=(it.correct||0)+1;it.mastery=Math.min(100,(it.mastery||0)+9);earned+=2;$("#fb").textContent="✦ Richtig"}else{it.wrong=(it.wrong||0)+1;it.mastery=Math.max(0,(it.mastery||0)-7);$("#fb").textContent="Das richtige Wort ist: "+it.text}state.history.push({childId:currentChild,itemId:it.id,mode:m,strategy,ok,at:now()});scheduleSync();n++;setTimeout(()=>n>=total?finish():draw(),950)}
function finish(){let c=child();if(c){c.stars=(c.stars||0)+earned;let gems=Math.max(2,Math.floor(earned/5));c.crystals=(c.crystals||0)+gems;let found=null;scheduleSync();$("#app").innerHTML=`<div class="finishScene"><div class="finishCard"><h1>Geschafft ✨</h1><p>⭐ +${earned} &nbsp; 💎 +${gems}</p>${found?`<div class="discover"><div>${found.icon}</div><b>Neu entdeckt: ${found.name}</b></div>`:""}<div class="row center" style="margin-top:18px"><button class="btn light" id="again">Noch eine Runde</button><button class="btn gold" id="worldFinish">Zur Zauberwelt</button></div></div></div>`;$("#again").onclick=()=>configureLearning(id);$("#worldFinish").onclick=()=>{route="world";render()}}}
draw()}
function world(){let c=child();return `<main class="worldPage"><section class="worldHero finalMap"><img src="./assets/zauberwelt-v3.png" class="worldImage" alt="Magische Zauberwelt"><div class="mapCounter">⭐ ${c?.stars||0}　💎 ${c?.crystals||0}</div>${Object.keys(AREAS).map(k=>`<button class="placeHot p-${k}" aria-label="${AREAS[k].name}" data-open-place="${k}"></button>`).join("")}</section><section class="worldBelow"><div><span class="tag">Deine Welt</span><h2>Wohin möchtest du?</h2><p>Wähle einen Ort direkt auf der Karte oder über die großen Schaltflächen darunter.</p></div><div class="areaChips">${Object.entries(AREAS).map(([k,a])=>`<button data-open-place="${k}">${a.icon} ${a.name}</button>`).join("")}</div></section></main>`}
function decorFor(area){return state.world.placed[area]||[]} function decorCards(area){let ids=decorFor(area);return ids.length?ids.map(id=>{let x=state.catalog.find(i=>i.id===id);return x?`<div class="placedDecor"><span>${x.icon}</span><b>${x.name}</b><button data-remove="${id}" data-area="${area}" title="Einpacken">×</button></div>`:""}).join(""):`<div class="emptyDecor">Noch keine Dekoration platziert.</div>`}
function houseInterior(){let room=state.world.room||"Wohnzimmer";let rooms=["Wohnzimmer","Schlafzimmer","Bibliothek","Zauberzimmer"];return `<div class="houseInterior"><div class="roomScene"><div class="windowGlow">☾</div><div class="beam b1"></div><div class="beam b2"></div><div class="roomTitle">${room}</div><div class="roomDecor">${decorCards("house")}</div></div><div class="roomTabs">${rooms.map(r=>`<button class="${r===room?'active':''}" data-room="${r}">${r}</button>`).join("")}</div></div>`}
function genericScene(area){let a=AREAS[area];return `<div class="areaScene scene-${area}"><div class="sceneGlow"></div><div class="sceneName">${a.icon} ${a.name}</div><div class="sceneDecor">${decorCards(area)}</div></div>`}
function placeView(area){selectedArea=area;let a=AREAS[area]||AREAS.clearing;state.world.area=area;return `<main class="page placePage"><button class="backBtn" data-route="world">← Zur Weltkarte</button><section class="placeHeader"><span class="tag">${a.icon} Ort</span><h1>${a.name}</h1><p>${a.desc}</p></section>${area==="house"?houseInterior():genericScene(area)}<section class="placeTools"><div><h2>Hier gestalten</h2><p>Gekaufte Gegenstände kannst du mit einem Klick an diesem Ort platzieren oder wieder einpacken.</p></div><button class="btn gold" data-route="design" data-design-area="${area}">Katalog öffnen</button></section><section class="quickCatalog">${state.catalog.filter(x=>state.world.owned.includes(x.id)&&(x.zone===area||x.zone==="all"||(area==="house"&&["house"].includes(x.zone))||(area==="garden"&&x.zone==="garden"))).map(x=>`<button class="decorChoice ${decorFor(area).includes(x.id)?'active':''}" data-toggle-decor="${x.id}" data-area="${area}"><span>${x.icon}</span>${x.name}<small>${decorFor(area).includes(x.id)?'platziert':'platzieren'}</small></button>`).join("")||`<div class="emptyDecor">Du besitzt für diesen Ort noch keine Dekoration. Öffne den Katalog.</div>`}</section></main>`}
function collectionView(){let c=child(),groups=[...new Set(state.collection.map(x=>x.type))];return `<main class="page collectionPage"><section class="magicHero compact"><span class="tag">✨ Magische Sammlung</span><h1>Deine Bewohner</h1><p>Beim Lernen verdienst du ⭐ Sterne. Damit kannst du selbst entscheiden, welche magischen Wesen du freischaltest.</p><div class="collectionWallet">⭐ ${c?.stars||0} Sterne verfügbar</div></section>${groups.map(g=>`<section class="collectionGroup"><h2>${g}</h2><div class="collectionGrid">${state.collection.filter(x=>x.type===g).map(x=>`<div class="creatureCard ${x.unlocked?'':'locked'}"><div class="creatureIcon">${x.unlocked?x.icon:'✨'}</div><b>${x.unlocked?x.name:'Geheimnisvolles Wesen'}</b><small>${x.rarity}</small>${x.unlocked?`<span class="ownedBadge">✓ Freigeschaltet</span>`:`<button class="btn starBuy" data-buy-creature="${x.id}">⭐ ${x.starCost} freischalten</button>`}</div>`).join("")}</div></section>`).join("")}</main>`}
function designView(){let c=child(),cats=["Alle",...new Set(state.catalog.map(x=>x.cat))];return `<main class="page designPage"><section class="magicHero compact"><span class="tag">🪄 Gestaltungskatalog</span><h1>Mach die Welt zu deiner.</h1><p>💎 ${c?.crystals||0} Kristalle verfügbar. Gekaufte Gegenstände bleiben dauerhaft im Inventar.</p></section><div class="catalogTabs">${cats.map(x=>`<button class="pill ${x===catalogCat?'active':''}" data-cat="${x}">${x}</button>`).join("")}</div><section class="catalogGrid">${state.catalog.filter(x=>catalogCat==="Alle"||x.cat===catalogCat).map(x=>`<article class="shopCard ${state.world.owned.includes(x.id)?'owned':''}"><div class="shopIcon">${x.icon}</div><h3>${x.name}</h3><p>${x.cat}</p><button class="btn ${state.world.owned.includes(x.id)?'light':''}" data-buy="${x.id}">${state.world.owned.includes(x.id)?'✓ Im Inventar':'💎 '+x.cost+' kaufen'}</button></article>`).join("")}</section><section class="inventoryPanel"><h2>Mein Inventar</h2><div class="inventory">${state.catalog.filter(x=>state.world.owned.includes(x.id)).map(x=>`<button data-inventory="${x.id}"><span>${x.icon}</span>${x.name}</button>`).join("")}</div></section></main>`}
function parent(){let c=child();return `<main class="page parentPage"><section class="magicHero compact"><span class="tag">⚙️ Elternbereich</span><h1>Verwalten & Überblick</h1><p>Profile, Lernwörter, Belohnungen, Testguthaben und Lernstand.</p></section><section class="parentGrid"><div class="card"><h2>Kinderprofile</h2>${state.children.map(x=>`<div class="row between profileRow"><button class="pill" data-child="${x.id}">${x.id===currentChild?'✓ ':''}${esc(x.name)}</button><button class="miniDanger" data-delete-child="${x.id}">Löschen</button></div>`).join("")}<button class="btn light" id="addChild">+ Profil</button></div><div class="card"><h2>Lernliste anlegen</h2><div class="field"><input id="title" placeholder="z. B. Ansage Woche 40"></div><div class="field"><textarea id="words" rows="6" placeholder="Ein Wort pro Zeile"></textarea></div><button class="btn" id="addSet">Speichern</button></div><div class="card"><h2>Belohnungen</h2>${state.rewards.map(r=>`<div class="row between rewardRow"><span>${esc(r.title)} · ⭐ ${r.cost}</span><button class="btn light" data-redeem="${r.id}">Einlösen</button></div>`).join("")||'<p class="small">Noch keine individuelle Belohnung.</p>'}<div class="field"><input id="reward" placeholder="z. B. Film aussuchen"></div><div class="field"><input id="cost" type="number" value="100" min="1"></div><button class="btn light" id="addReward">+ Belohnung</button></div><div class="card"><h2>Testen & Cloud</h2><p class="small">Für die Testphase kannst du Kristalle nachladen.</p><button class="btn gold" id="testGems">💎 +500 Test-Kristalle</button><hr>${cloud()}</div></section><h2 class="sectionTitleText">${c?esc(c.name)+' · Lernstand':'Noch kein Profil'}</h2><section class="learnStatusGrid">${sets().map(s=>`<div class="card"><div class="row between"><h3>${esc(s.title)}</h3><button class="miniDanger" data-delete-set="${s.id}">Liste löschen</button></div>${s.items.map(i=>`<div class="statusWord"><div class="row between"><b>${esc(i.text)}</b><span>${Math.round(i.mastery)}%</span></div><div class="progress"><i style="width:${i.mastery}%"></i></div></div>`).join("")}</div>`).join("")}</section></main>`}
function cloud(){if(demo)return `<p class="small">Lokaler Modus.</p>`;if(session)return `<span class="tag">✓ ${esc(session.user.email||'angemeldet')}</span><p class="small">Änderungen werden automatisch synchronisiert.</p><button class="btn light" id="logout">Abmelden</button>`;return `<div class="field"><input id="email" type="email" placeholder="E-Mail"></div><div class="field"><input id="pw" type="password" placeholder="Passwort"></div><div class="row"><button class="btn" id="login">Anmelden</button><button class="btn light" id="signup">Konto erstellen</button></div>`}
function bind(){document.querySelectorAll("[data-route]").forEach(b=>b.onclick=(ev)=>{ev.preventDefault();let target=b.dataset.route;if(target==="world"){selectedArea=state.world?.area||"clearing";route="world";try{render()}catch(err){console.error("Zauberwelt:",err);$("#app").innerHTML=shell(`<main class="page"><section class="magicHero"><h1>🌳 Zauberwelt</h1><p>Die Weltkarte konnte nicht vollständig geladen werden.</p><div class="areaChips">${Object.entries(AREAS).map(([k,a])=>`<button data-open-place="${k}">${a.icon} ${a.name}</button>`).join("")}</div></section></main>`);bind()}return}route=target;render()});document.querySelectorAll("[data-child]").forEach(b=>b.onclick=()=>{currentChild=b.dataset.child;render()});document.querySelectorAll("[data-configure]").forEach(b=>b.onclick=()=>configureLearning(b.dataset.configure));document.querySelectorAll("[data-cat]").forEach(b=>b.onclick=()=>{catalogCat=b.dataset.cat;render()});document.querySelectorAll("[data-open-place]").forEach(b=>b.onclick=()=>{selectedArea=b.dataset.openPlace;state.world.area=selectedArea;scheduleSync();route="place";render()});document.querySelectorAll("[data-room]").forEach(b=>b.onclick=()=>{state.world.room=b.dataset.room;scheduleSync();render()});document.querySelectorAll("[data-toggle-decor]").forEach(b=>b.onclick=()=>toggleDecor(b.dataset.area,b.dataset.toggleDecor));document.querySelectorAll("[data-remove]").forEach(b=>b.onclick=()=>toggleDecor(b.dataset.area,b.dataset.remove));document.querySelectorAll("[data-buy]").forEach(b=>b.onclick=()=>buy(b.dataset.buy));document.querySelectorAll("[data-buy-creature]").forEach(b=>b.onclick=()=>buyCreature(b.dataset.buyCreature));$("#testGems")?.addEventListener("click",()=>{let c=child();if(!c)return alert("Bitte zuerst ein Kinderprofil wählen.");c.crystals=(c.crystals||0)+500;scheduleSync();render()});$("#addChild")?.addEventListener("click",()=>{let n=prompt("Name des Kinderprofils:");if(n?.trim()){let x={id:uid(),name:n.trim(),stars:0,crystals:1500};state.children.push(x);currentChild=x.id;scheduleSync();render()}});document.querySelectorAll("[data-delete-child]").forEach(b=>b.onclick=()=>{let x=state.children.find(c=>c.id===b.dataset.deleteChild);if(x&&confirm(`Profil "${x.name}" und seine Lernlisten wirklich löschen?`)){state.children=state.children.filter(c=>c.id!==x.id);state.sets=state.sets.filter(s=>s.childId!==x.id);currentChild=state.children[0]?.id||null;scheduleSync();render()}});$("#addSet")?.addEventListener("click",()=>{if(!currentChild)return alert("Bitte zuerst ein Kinderprofil anlegen.");let title=$("#title").value.trim(),words=[...new Set($("#words").value.split(/[\n,;]+/).map(x=>x.trim()).filter(Boolean))];if(!title||!words.length)return alert("Titel und Lernwörter eingeben.");state.sets.push({id:uid(),childId:currentChild,subjectId:"de",title,items:words.map(text=>({id:uid(),text,mastery:10,seen:0,correct:0,wrong:0,last:null}))});scheduleSync();render()});document.querySelectorAll("[data-delete-set]").forEach(b=>b.onclick=()=>{if(confirm("Diese Lernliste löschen?")){state.sets=state.sets.filter(s=>s.id!==b.dataset.deleteSet);scheduleSync();render()}});$("#addReward")?.addEventListener("click",()=>{let t=$("#reward").value.trim(),cost=Number($("#cost").value);if(t&&cost>0){state.rewards.push({id:uid(),title:t,cost});scheduleSync();render()}});document.querySelectorAll("[data-redeem]").forEach(b=>b.onclick=()=>{let r=state.rewards.find(x=>x.id===b.dataset.redeem),c=child();if(!c)return;if((c.stars||0)<r.cost)return alert("Noch nicht genug Sterne.");if(confirm(`${r.title} einlösen?`)){c.stars-=r.cost;scheduleSync();render()}});$("#logout")?.addEventListener("click",async()=>{await supabase.auth.signOut();session=null;render()});$("#login")?.addEventListener("click",()=>auth(false));$("#signup")?.addEventListener("click",()=>auth(true))}
function buyCreature(id){let x=state.collection.find(i=>i.id===id),c=child();if(!x||!c)return alert("Bitte zuerst ein Kinderprofil wählen.");if(x.unlocked)return;if((c.stars||0)<x.starCost)return alert(`Dir fehlen noch ${x.starCost-(c.stars||0)} Sterne.`);if(!confirm(`${x.name} für ⭐ ${x.starCost} freischalten?`))return;c.stars-=x.starCost;x.unlocked=true;scheduleSync();render()}
function buy(id){let x=state.catalog.find(i=>i.id===id),c=child();if(!c)return alert("Bitte zuerst Kinderprofil wählen.");if(state.world.owned.includes(id))return alert("Dieser Gegenstand ist bereits im Inventar.");if((c.crystals||0)<x.cost)return alert("Noch nicht genug Kristalle.");c.crystals-=x.cost;state.world.owned.push(id);scheduleSync();render()}
function toggleDecor(area,id){let arr=state.world.placed[area]||(state.world.placed[area]=[]);let i=arr.indexOf(id);if(i>=0)arr.splice(i,1);else arr.push(id);scheduleSync();render()}
async function auth(signup){let email=$("#email").value.trim(),password=$("#pw").value;let r=signup?await supabase.auth.signUp({email,password}):await supabase.auth.signInWithPassword({email,password});if(r.error)return alert(r.error.message);session=r.data.session;if(session){await pullCloud();render()}else alert("Bitte ggf. Bestätigungs-E-Mail öffnen.")}
await initCloud();normalize();saveLocal();render();
